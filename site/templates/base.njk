<!DOCTYPE html>
<html lang="en" {% if pageClass %} id="page-{{pageClass}}"{% elseif page.fileSlug %} id="page-{{page.fileSlug}}"{% else %} id="page-index"{% endif %}>
	<head>
		<meta charset="UTF-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">     
		<title>{% include "header/uniqueTitle.njk" %}</title>

		{% include "header/social.njk" %}

		{% if noindex %}
		<meta name="robots" content="noindex" />
		{% endif %}

		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Bitcount+Prop+Single:wght@100..900&family=Boldonse&family=Quicksand:wght@300..700&display=swap" rel="stylesheet">
		<link rel="stylesheet" href="{{ '/style/style.css' | url }}">
	</head>
	<body>

		{% include 'header.njk' %}
		<main>
			{{ content | safe }}
		</main>

		<script src="./scripts/app.js"></script>
	</body>
</html>
